[#boost-asio-basic_streambuf-size]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/basic_streambuf.adoc[basic&lowbar;streambuf]::size
:relfileprefix: ../../../
:mrdocs:

Get the size of the input sequence&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;basic&lowbar;streambuf&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
size&lowbar;t
size() const noexcept;
----

== Description

[,cpp]
----
size_t s = 0;
const_buffers_type bufs = data();
const_buffers_type::const_iterator i = bufs.begin();
while (i != bufs.end())
{
  const_buffer buf(*i++);
  s += buf.size();
}
----
== Return Value

The size of the input sequence&period; The value is equal to that calculated for `s` in the following code&colon;


[.small]#Created with https://www.mrdocs.com[MrDocs]#
