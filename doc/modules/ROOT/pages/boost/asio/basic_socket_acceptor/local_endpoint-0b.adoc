[#boost-asio-basic_socket_acceptor-local_endpoint-0b]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/basic_socket_acceptor.adoc[basic&lowbar;socket&lowbar;acceptor]::local&lowbar;endpoint
:relfileprefix: ../../../
:mrdocs:

Get the local endpoint of the acceptor&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;basic&lowbar;socket&lowbar;acceptor&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
xref:boost/asio/basic_socket_acceptor/endpoint_type.adoc[endpoint&lowbar;type]
local&lowbar;endpoint(boost::system::error&lowbar;code& ec) const;
----

== Description

This function is used to obtain the locally bound endpoint of the acceptor&period;

== Example
[,cpp]
----
boost::asio::ip::tcp::acceptor acceptor(my_context);
...
boost::system::error_code ec;
boost::asio::ip::tcp::endpoint endpoint = acceptor.local_endpoint(ec);
if (ec)
{
  // An error occurred.
}
----
== Return Value

An object that represents the local endpoint of the acceptor&period; Returns a default&hyphen;constructed endpoint object if an error occurred and the error handler did not throw an exception&period;

== Parameters

[cols=2]
|===
| Name
| Description
| *ec*
| Set to indicate what error occurred, if any&period;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
