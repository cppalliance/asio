[#boost-asio-basic_socket_acceptor-bind-0d]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/basic_socket_acceptor.adoc[basic&lowbar;socket&lowbar;acceptor]::bind
:relfileprefix: ../../../
:mrdocs:

Bind the acceptor to the given local endpoint&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;basic&lowbar;socket&lowbar;acceptor&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
void
bind(xref:boost/asio/basic_socket_acceptor/endpoint_type.adoc[endpoint&lowbar;type] const& endpoint);
----

== Description

This function binds the socket acceptor to the specified endpoint on the local machine&period;

== Example
[,cpp]
----
boost::asio::ip::tcp::acceptor acceptor(my_context);
boost::asio::ip::tcp::endpoint endpoint(boost::asio::ip::tcp::v4(), 12345);
acceptor.open(endpoint.protocol());
acceptor.bind(endpoint);
----
== Exceptions

[cols=2]
|===
| Name
| Thrown on
| `boost&colon;&colon;system&colon;&colon;system&lowbar;error`
| Thrown on failure&period;
|===

== Parameters

[cols=2]
|===
| Name
| Description
| *endpoint*
| An endpoint on the local machine to which the socket acceptor will be bound&period;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
