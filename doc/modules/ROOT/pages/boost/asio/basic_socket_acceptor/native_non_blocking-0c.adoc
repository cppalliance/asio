[#boost-asio-basic_socket_acceptor-native_non_blocking-0c]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/basic_socket_acceptor.adoc[basic&lowbar;socket&lowbar;acceptor]::native&lowbar;non&lowbar;blocking
:relfileprefix: ../../../
:mrdocs:

`native&lowbar;non&lowbar;blocking` overloads

== Synopses

Declared in `&lt;boost&sol;asio&sol;basic&lowbar;socket&lowbar;acceptor&period;hpp&gt;`

Gets the non&hyphen;blocking mode of the native acceptor implementation&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
bool
xref:boost/asio/basic_socket_acceptor/native_non_blocking-04d.adoc[native&lowbar;non&lowbar;blocking]() const;
----

[.small]#xref:boost/asio/basic_socket_acceptor/native_non_blocking-04d.adoc[_» more&period;&period;&period;_]#

Sets the non&hyphen;blocking mode of the native acceptor implementation&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
void
xref:boost/asio/basic_socket_acceptor/native_non_blocking-043.adoc[native&lowbar;non&lowbar;blocking](bool mode);
----

[.small]#xref:boost/asio/basic_socket_acceptor/native_non_blocking-043.adoc[_» more&period;&period;&period;_]#

Sets the non&hyphen;blocking mode of the native acceptor implementation&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
boost::system::error&lowbar;code
xref:boost/asio/basic_socket_acceptor/native_non_blocking-09.adoc[native&lowbar;non&lowbar;blocking](
    bool mode,
    boost::system::error&lowbar;code& ec);
----

[.small]#xref:boost/asio/basic_socket_acceptor/native_non_blocking-09.adoc[_» more&period;&period;&period;_]#

== Exceptions

[cols=2]
|===
| Name
| Thrown on
| `boost&colon;&colon;system&colon;&colon;system&lowbar;error`
| Thrown on failure&period; If the `mode` is `false,` but the current value of `non&lowbar;blocking()` is `true,` this function fails with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;invalid&lowbar;argument, as the combination does not make sense&period;
|===

== Return Value

`true` if the underlying acceptor is in non&hyphen;blocking mode and direct system calls may fail with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;would&lowbar;block (or the equivalent system error)&period;

== Parameters

[cols=2]
|===
| Name
| Description
| *mode*
| If `true,` the underlying acceptor is put into non&hyphen;blocking mode and direct system calls may fail with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;would&lowbar;block (or the equivalent system error)&period;
| *ec*
| Set to indicate what error occurred, if any&period; If the `mode` is `false,` but the current value of `non&lowbar;blocking()` is `true,` this function fails with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;invalid&lowbar;argument, as the combination does not make sense&period;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
