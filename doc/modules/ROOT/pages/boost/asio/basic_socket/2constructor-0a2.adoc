[#boost-asio-basic_socket-2constructor-0a2]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/basic_socket.adoc[basic&lowbar;socket]::basic&lowbar;socket
:relfileprefix: ../../../
:mrdocs:

Move&hyphen;construct a basic&lowbar;socket from a socket of another protocol type&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;basic&lowbar;socket&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
template&lt;
    typename Protocol1,
    typename Executor1&gt;
basic&lowbar;socket(
    xref:boost/asio/basic_socket.adoc[basic&lowbar;socket&lt;Protocol1, Executor1&gt;]&& other,
    int = 0);
----

== Description

This constructor moves a socket from one object to another&period;

[NOTE]
Following the move, the moved&hyphen;from object is in the same state as if constructed using the `basic&lowbar;socket(const` executor&lowbar;type&amp;) constructor&period;

== Parameters

[cols=2]
|===
| Name
| Description
| *other*
| The other basic&lowbar;socket object from which the move will occur&period;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
