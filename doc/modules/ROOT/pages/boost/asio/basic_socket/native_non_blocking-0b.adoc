[#boost-asio-basic_socket-native_non_blocking-0b]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/basic_socket.adoc[basic&lowbar;socket]::native&lowbar;non&lowbar;blocking
:relfileprefix: ../../../
:mrdocs:

`native&lowbar;non&lowbar;blocking` overloads

== Synopses

Declared in `&lt;boost&sol;asio&sol;basic&lowbar;socket&period;hpp&gt;`

Gets the non&hyphen;blocking mode of the native socket implementation&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
bool
xref:boost/asio/basic_socket/native_non_blocking-050.adoc[native&lowbar;non&lowbar;blocking]() const;
----

[.small]#xref:boost/asio/basic_socket/native_non_blocking-050.adoc[_» more&period;&period;&period;_]#

Sets the non&hyphen;blocking mode of the native socket implementation&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
void
xref:boost/asio/basic_socket/native_non_blocking-059.adoc[native&lowbar;non&lowbar;blocking](bool mode);
----

[.small]#xref:boost/asio/basic_socket/native_non_blocking-059.adoc[_» more&period;&period;&period;_]#

Sets the non&hyphen;blocking mode of the native socket implementation&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
boost::system::error&lowbar;code
xref:boost/asio/basic_socket/native_non_blocking-0c.adoc[native&lowbar;non&lowbar;blocking](
    bool mode,
    boost::system::error&lowbar;code& ec);
----

[.small]#xref:boost/asio/basic_socket/native_non_blocking-0c.adoc[_» more&period;&period;&period;_]#

== Exceptions

[cols=2]
|===
| Name
| Thrown on
| `boost&colon;&colon;system&colon;&colon;system&lowbar;error`
| Thrown on failure&period; If the `mode` is `false,` but the current value of `non&lowbar;blocking()` is `true,` this function fails with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;invalid&lowbar;argument, as the combination does not make sense&period;
|===

== Return Value

`true` if the underlying socket is in non&hyphen;blocking mode and direct system calls may fail with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;would&lowbar;block (or the equivalent system error)&period;

== Parameters

[cols=2]
|===
| Name
| Description
| *mode*
| If `true,` the underlying socket is put into non&hyphen;blocking mode and direct system calls may fail with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;would&lowbar;block (or the equivalent system error)&period;
| *ec*
| Set to indicate what error occurred, if any&period; If the `mode` is `false,` but the current value of `non&lowbar;blocking()` is `true,` this function fails with boost&colon;&colon;asio&colon;&colon;error&colon;&colon;invalid&lowbar;argument, as the combination does not make sense&period;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
