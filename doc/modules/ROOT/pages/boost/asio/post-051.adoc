[#boost-asio-post-051]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::post
:relfileprefix: ../../
:mrdocs:

Submits a completion token or function object for execution&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;post&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
template&lt;xref:boost/asio/completion_token_for.adoc[boost::asio::completion&lowbar;token&lowbar;for&lt;void()&gt;] NullaryToken&gt;
decltype(async&lowbar;initiate&lt;NullaryToken, void()&gt;(
      declval&lt;detail&colon;&colon;initiate&lowbar;post&gt;(), token))
post(NullaryToken&& token);
----

== Description

This function submits an object for execution using the object&apos;s associated executor&period; The function object is queued for execution, and is never called from the current thread prior to returning from &period;

The use of `post(),` rather than xref:boost/asio/defer-0c2.adoc[`defer()`], indicates the caller&apos;s preference that the function object be eagerly queued for execution&period;

[,cpp]
----
void handler(); 
----
[,cpp]
----
class Init
{
public:
  template <typename CompletionHandler>
    void operator()(CompletionHandler&& completion_handler) const;
}; 
----
The function call operator of `Init&colon;`

* Obtains the handler&apos;s associated executor object `ex` of type `Ex` by performing

[,cpp]
----
auto ex = get_associated_executor(handler); 
----
* Obtains the handler&apos;s associated allocator object `alloc` by performing

[,cpp]
----
auto alloc = get_associated_allocator(handler); 
----
* If is true, performs

[,cpp]
----
prefer(
    require(ex, execution::blocking.never),
    execution::relationship.fork,
    execution::allocator(alloc)
  ).execute(std::forward<CompletionHandler>(completion_handler)); 
----
* If is false, performs

[,cpp]
----
ex.post(
    std::forward<CompletionHandler>(completion_handler),
    alloc); 
----
== Completion Signature
[,cpp]
----
void() 
----
== Return Value

This function returns , where `Init` is a function object type defined as&colon;

== Parameters

[cols=2]
|===
| Name
| Description
| *token*
| The `completion&lowbar;token` that will be used to produce a completion handler&period; The function signature of the completion handler must be&colon;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
