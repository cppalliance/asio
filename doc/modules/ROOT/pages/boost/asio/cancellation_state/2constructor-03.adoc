[#boost-asio-cancellation_state-2constructor-03]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/cancellation_state.adoc[cancellation&lowbar;state]::cancellation&lowbar;state
:relfileprefix: ../../../
:mrdocs:

Constructors

== Synopses

Declared in `&lt;boost&sol;asio&sol;cancellation&lowbar;state&period;hpp&gt;`

Construct a disconnected cancellation state&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
constexpr
xref:boost/asio/cancellation_state/2constructor-0b.adoc[cancellation&lowbar;state]() noexcept;
----

[.small]#xref:boost/asio/cancellation_state/2constructor-0b.adoc[_» more&period;&period;&period;_]#

Construct and attach to a parent slot to create a new child slot&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
template&lt;typename CancellationSlot&gt;
constexpr
xref:boost/asio/cancellation_state/2constructor-01.adoc[cancellation&lowbar;state](CancellationSlot slot);
----

[.small]#xref:boost/asio/cancellation_state/2constructor-01.adoc[_» more&period;&period;&period;_]#

Construct and attach to a parent slot to create a new child slot&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
template&lt;
    typename CancellationSlot,
    typename Filter&gt;
constexpr
xref:boost/asio/cancellation_state/2constructor-0c.adoc[cancellation&lowbar;state](
    CancellationSlot slot,
    Filter filter);
----

[.small]#xref:boost/asio/cancellation_state/2constructor-0c.adoc[_» more&period;&period;&period;_]#

Construct and attach to a parent slot to create a new child slot&period;


[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
template&lt;
    typename CancellationSlot,
    typename InFilter,
    typename OutFilter&gt;
constexpr
xref:boost/asio/cancellation_state/2constructor-0d.adoc[cancellation&lowbar;state](
    CancellationSlot slot,
    InFilter in&lowbar;filter,
    OutFilter out&lowbar;filter);
----

[.small]#xref:boost/asio/cancellation_state/2constructor-0d.adoc[_» more&period;&period;&period;_]#

== Parameters

[cols=2]
|===
| Name
| Description
| *slot*
| The parent cancellation slot to which the state will be attached&period;
| *filter*
| A function object that is used to transform incoming cancellation signals as they are received from the parent slot&period; This function object must have the signature&colon;
| *in&lowbar;filter*
| A function object that is used to transform incoming cancellation signals as they are received from the parent slot&period; This function object must have the signature&colon;
| *out&lowbar;filter*
| A function object that is used to transform outcoming cancellation signals as they are relayed to the child slot&period; This function object must have the signature&colon;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
