[#boost-asio-io_context-basic_executor_type-post]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/io_context.adoc[io&lowbar;context]::xref:boost/asio/io_context/basic_executor_type.adoc[basic&lowbar;executor&lowbar;type]::post
:relfileprefix: ../../../../
:mrdocs:

Request the io&lowbar;context to invoke the given function object&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;io&lowbar;context&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
template&lt;
    typename Function,
    typename OtherAllocator&gt;
void
post(
    Function&& f,
    OtherAllocator const& a);
----

== Description

This function is used to ask the io&lowbar;context to execute the given function object&period; The function object will never be executed inside `post()&period;` Instead, it will be scheduled to run on the io&lowbar;context&period;

[,cpp]
----
void function(); 
----
== Parameters

[cols=2]
|===
| Name
| Description
| *f*
| The function object to be called&period; The executor will make a copy of the handler object as required&period; The function signature of the function object must be&colon;
| *a*
| An allocator that may be used by the executor to allocate the internal storage needed for function invocation&period;
|===


[.small]#Created with https://www.mrdocs.com[MrDocs]#
