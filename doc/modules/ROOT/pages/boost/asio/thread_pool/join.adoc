[#boost-asio-thread_pool-join]
= xref:boost.adoc[boost]::xref:boost/asio.adoc[asio]::xref:boost/asio/thread_pool.adoc[thread&lowbar;pool]::join
:relfileprefix: ../../../
:mrdocs:

Joins the threads&period;

== Synopsis

Declared in `&lt;boost&sol;asio&sol;thread&lowbar;pool&period;hpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
void
join();
----

== Description

This function blocks until the threads in the pool have completed&period; If `stop()` is not called prior to `join(),` the `join()` call will wait until the pool has no more outstanding work&period;


[.small]#Created with https://www.mrdocs.com[MrDocs]#
